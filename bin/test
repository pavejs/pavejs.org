#!/bin/sh -e

# Initialize nginx server
bin/run &
while ! curl -fs $WEBSITE_URL >/dev/null 2>&1; do
  # Attempts to curl .env website URL, sleeping with return code of 1 on failure
  sleep 1
done

# Kill nginx process 
kill $(cat /usr/local/nginx/logs/nginx.pid)